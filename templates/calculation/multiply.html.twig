{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    <div>
        <strong>Result:</strong> <span id="calculation_result">{{ result }}</span>
    </div>
{% endblock %}
